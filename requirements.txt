# requirements.txt — TruthTeller AI backend
# Install with: pip install -r requirements.txt

# Core ML
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# NLP
nltk>=3.8

# PDF parsing — pypdf replaces the abandoned PyPDF2
pypdf>=4.0          # replaces PyPDF2 (drop-in compatible)
# PyPDF2            # REMOVED — abandoned, causes ModuleNotFoundError on new installs

# Document parsing
python-docx>=1.1
python-pptx>=0.6

# Web framework
flask>=3.0
flask-cors>=4.0
werkzeug>=3.0

# Serialisation
joblib>=1.3

# Data downloading
datasets>=2.0
requests>=2.0